import React from "react";
import { FormEditorProps } from "./types";

const FormEditor: React.FunctionComponent<FormEditorProps> = (props) => {
    return (
        <div>
            Form Editor
        </div>
    );
};

export default FormEditor;