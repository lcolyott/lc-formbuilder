import React from "react";
import { FormViewerProps } from "./types";

const FormViewer: React.FunctionComponent<FormViewerProps> = (props) => {
    return (
        <div>
            Form Viewer
        </div>
    );
};

export default FormViewer;