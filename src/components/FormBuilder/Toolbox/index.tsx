import React from "react";

const Toolbox: React.FunctionComponent<any> = (props) => {
    return (
        <div style={{ gridArea: "toolbox", border: "1px solid lightgray" }}>

        </div>
    );
};

export default Toolbox;