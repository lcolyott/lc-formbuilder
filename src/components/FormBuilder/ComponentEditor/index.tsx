import { withStyles } from "@material-ui/core";
import React from "react";

const ComponentEditor = withStyles({})((props) => {
    return (
        <div>
            Component Editor
        </div>
    );
});

export default ComponentEditor;